/** Cantidad de filas por página en listados (paquetes, reservas). */
export const ROWS_PER_PAGE = 5;

/** Longitud máxima para mostrar descripción en tablas (truncate). */
export const MAX_DESC_LENGTH = 25;

/** Longitud máxima para mostrar nombre en listado de paquetes. */
export const MAX_NOMBRE_LENGTH = 25;

/** Longitud máxima para mostrar nombre del paquete en listado de reservas. */
export const MAX_NOMBRE_PAQUETE_LENGTH = 25;

/** Valor del select "Agregar otra agencia" en el paso de agente (reserva). */
export const AGENTE_AGREGAR_OTRA_VALUE = '__nueva__';
